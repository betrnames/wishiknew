# Archive

- `MemeApp.v0.tsx` — frozen snapshot of the regret→meme generator before the discovery-feed pivot.
- Live optional UI: `src/modes/MemeLab.tsx` (opened from a quiet “Meme lab” link, not the homepage hero).

Do not delete without copying elsewhere — this is the “save a version” backup.
